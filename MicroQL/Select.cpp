#include "stdafx.h"
#include "Select.h"
#include "QueryPlan.h"
#include <string>

Select :: Select(){ }
/*
Select :: ResultTable evaluateQuery(string query)
{
	// store query

	// *** parse the query ***
	// split the query
	// if(!parseSyntax())
	//	 throw exception

	// if(!parseSchema())
	//	 throw exception


	// *** generate the query plan ***
	// generateQueryPlan()


	// *** evaluate the query ***
	// create empty ResultTable and have a pointer point to it
	ResultTable resultTable = new ResultTable();
	ResultTable * resultTablePtr = &resultTable;

	// if there is where clause
	//		evaluateWhere(resultTablePtr);
	// if there is join clause
	//		evaluateJoin(resultTablePtr);
	// evaluateSelect(resultTablePtr);

	// *** return a copy of the resultTable values ***
	return resultTable;

}
Select :: QueryPlan generateQueryPlan()
{
	// create a query plan
	QueryPlan queryPlan;
	queryPlans.insert(queryPlan);

	// loop through the 3 lists and add to query plan
	// addProjection(columnName);
	// addSelection(tableName);
	// addCondition(conditionList);
}
*/